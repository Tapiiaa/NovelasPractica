<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.novelaspractica">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">

        <!-- Declaración de la actividad de inicio -->
        <activity
            android:name="com.example.novelaspractica.activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Declaración de la MainActivity -->
        <activity
            android:name="com.example.novelaspractica.activities.MainActivity"
            android:exported="true" />

        <!-- Declaración de otras actividades si existen -->
        <activity
            android:name="com.example.novelaspractica.activities.AddReviewActivity"
            android:exported="true" />

        <activity
            android:name="com.example.novelaspractica.activities.ShowNovelsActivity"
            android:exported="true" />

        <activity
            android:name="com.example.novelaspractica.activities.ShowReviewsActivity"
            android:exported="true" />

    </application>

</manifest>
